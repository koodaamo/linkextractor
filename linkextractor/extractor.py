import HTMLParser, logging
from BeautifulSoup import BeautifulSoup, SoupStrainer
from urlparse import urlparse
from lxml import

import requests


def from_html(documents, validate=False, fix=False, expand=False):
   "given an iterable producing documents, extract links from them"


def from_text(documents):
   "given  an iterable producing documents, extract links from them"
